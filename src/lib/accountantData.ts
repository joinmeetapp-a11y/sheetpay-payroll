import {
  AccountantClient,
  AttentionItem,
  BatchPayrollJob,
  ClientInvitation,
  Employee,
  FirmTeamMember,
  PayrollRun,
} from '../types';

export const initialAccountantClients: AccountantClient[] = [];

export const initialFirmTeamMembers: FirmTeamMember[] = [];

export const initialAttentionItems: AttentionItem[] = [];

export const initialBatchJobs: BatchPayrollJob[] = [];
